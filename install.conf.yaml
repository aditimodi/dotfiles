- defaults:
    link:
      relink: true

- clean: ['~']

- create:

- shell:
   - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.vimrc: vimrc
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vim:
    ~/.aliases:
    ~/.exports:
    ~/.functions:
    ~/.gitconfig: gitconfig
